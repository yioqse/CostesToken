# Creacion interfaz grafica

InterfazEmpresa.png 
Esta es una réplica funcional de la interfaz que usa la empresa construida con Tkinter (la librería estándar de Python para GUIs) para evitar dependencias pesadas, pero diseñada para que coincida visualmente con los colores, secciones y el flujo de tu imagen.

He integrado el PipelineCostes que desarrollamos para que el botón "Calcular" use tu lógica de IA local (Transformers).

el codigo: demo_gui.py es una replica exacta pero no funciona el boton calcular costes